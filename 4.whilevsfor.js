// while loops
var numWhile = 0;

while(numWhile < 5) {
    numWhile +=1;
    console.log(numWhile + " while");
}

// for loop
for(let numFor = 0; numFor <= 5;numFor++) {
    console.log(numFor + ' for loop');
}